# Copy to .dev.vars (gitignored) and fill in real values for local dev.
# Cloudflare's local Workers runtime reads .dev.vars automatically —
# no other config needed. For production, use `wrangler secret put`.

OPENAI_API_KEY=sk-...
